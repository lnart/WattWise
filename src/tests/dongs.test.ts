
import { promiseImpl } from 'ejs'
import * as reader from '../controller/countController'
jest.mock('../controller/countController.ts')




describe('reads counts', () => {
    test('should return liveCount', () => {

    })
})
